import React from 'react'

const Button = ({height,width}) => {
  return (
    <div>

       <button className={`${primary?" bg-pink ":""} `}>

       </button>
    </div>
  )
}

export default Button